# Changelog

## v0.3.0 - Feature Parity (WIP)

### Added
- Browser automation tool (navigate, screenshot, extract)
- File write/edit/delete tool with safe root + backups
- System exec tool with allowlist and confirmations
- Memory system with prompt context + auto-append
- Multi-agent sessions with persistence

### Improved
- Admin auto-grant on pairing with admin token
- Install flow creates admin pairing token

### Tests
- Unit tests for files_write, system_exec, memory, sessions
